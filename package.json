{
  "name": "03-edu_portal",
  "version": "1.0.0",
  "description": "",
  "main": "setup_client.js",
  "scripts": {
    "prepare": "husky",
    "lint:commits": "commitlint --from HEAD~5 --to HEAD --verbose",
    "test": "echo \"No tests yet\" && exit 0",
    "dev:frontend": "npm run start -w frontend",
    "dev:server": "npm run start -w server",
    "dev": "concurrently -n \"SRV,UI\" -c \"blue,cyan\" \"npm run dev:server\" \"npm run dev:frontend\"",
    "build:all": "npm run build -w frontend && npm run build -w server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "concurrently": "^10.0.5",
    "husky": "^9.1.7"
  },
    "workspaces": ["server", "frontend"]
}
